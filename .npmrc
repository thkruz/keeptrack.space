# pnpm settings (node-linker, strict-peer-dependencies, auto-install-peers) moved
# to pnpm-workspace.yaml so that npm/npx don't warn "Unknown config" about them.
# Keep this file free of pnpm-only keys; add only npm-compatible keys here.

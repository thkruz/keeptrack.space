/* __DISPLAY_NAME__ (overlay/toggle plugin).
 *
 * Toggle plugins usually need little or no CSS. Add rules here if your overlay
 * renders its own DOM. This file is imported from plugin.ts so it is bundled. */
